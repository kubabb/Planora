{
  "name": "planora",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b && npm run build --workspace planora-web",
    "build:ts": "tsc -b",
    "build:web": "npm run build --workspace planora-web",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4",
    "vitest": "^4.1.7"
  }
}
